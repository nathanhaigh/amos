#ifndef __AMP_HH
#define __AMP_HH

#include <ctime>
#include <cstring>

const char * Date ( );

inline long long int RevC (long long int c, long long int len)
{ return len - c + 1; }

#endif // _AMP_HH
