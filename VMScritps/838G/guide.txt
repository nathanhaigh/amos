Server configuration:

1. $ gcc -m32 -o httpd httpd.c
2. $ execstack -s httpd
3. $ setarch i386 -R ./httpd
httpd running on port 56927

Client configuration:
(open another terminal on the same machine with the server)

1. $ gcc -m32 -o exploit exploit.c
2. $ ./exploit (type the server's port here) 

e.g: ./exploit 56927